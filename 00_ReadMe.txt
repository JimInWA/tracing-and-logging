1. Open the 01_WCF_Service_SimpleMathService.sln file and run the site (non-debug) so the WCF Service is up and running
- Code for this service is in 01_WCF_Service\SimpleMathService

2. The Proxy Class (Service Class) for the above WCF Service has been generated and is in 01a_ProxyClass folder

3. Open the 02_ClientApplication_SimpleMathClient.sln file and run the site (non-debug) so the Client Application is up and running

4. Notice that there is an error in the addition - this is to simulate the basic troubleshooting process; in other words, what layer is the issue in - the UI layer or the Service layer

